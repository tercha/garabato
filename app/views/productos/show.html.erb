<p id="notice"><%= notice %></p>

<p>
  <strong>Logo:</strong>
  <%= image_tag(@producto.logo_url, :width => 600) if @producto.logo.present? %>
</p>

<p>
  <strong>Categoria:</strong>
  <%= @producto.categoria %>
</p>

<p>
  <strong>Nombre:</strong>
  <%= @producto.nombre %>
</p>

<%= link_to 'Edit', edit_producto_path(@producto) %> |
<%= link_to 'Back', productos_path %>
